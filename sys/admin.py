from email.headerregistry import UniqueAddressHeader
from os import PRIO_USER
import shutil

from httplib2 import ServerNotFoundError
from obj1 import User as user
from obj1 import term_resized_user
password = 'admin'

class super_user:
        def __init__(self,super):
            self.super = super
        
        def run_as_administator(self):
            if user is True:
                input == "insert admin password"
                if password =='admin':
                    UniqueAddressHeader is True
                    PRIO_USER is True     
            def delete_a_directory(self):
                if input() == "delete directory {}".format():
                    shutil.rmtree("bootloader")   
        def scale_permission(self):
            while super_user is True:
                if input == "scale_privileges":
                    term_resized_user(user)
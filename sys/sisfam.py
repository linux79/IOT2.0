from asyncio.proactor_events import BaseProactorEventLoop
from tarfile import is_tarfile
from sys_gui import Gui as gui
class System:
    isafile = True
    def tgui (self):
        if is_tarfile is object:
            BaseProactorEventLoop(any)
    
    